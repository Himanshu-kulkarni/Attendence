# Database Configuration
# For local SQLite: leave blank or omit (falls back to attendance.db automatically)
# For local/prod PostgreSQL: postgresql://username:password@localhost:5432/dbname
DATABASE_URL=
